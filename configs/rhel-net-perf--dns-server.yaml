document: feedback-pipeline-workload
version: 1
data:
  name: DNS Server
  description: Popular implementations of DNS authoritative servers and resolvers
  maintainer: rhel-net-perf
  packages:
    - bind
    - bind-chroot
    - dnsmasq
    - unbound
    - unbound-utils
  labels:
    - eln
    - c10s
